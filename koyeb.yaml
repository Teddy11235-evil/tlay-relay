name: render-relay
services:
  - name: app
    type: web
    env:
      - name: PORT
        value: "8000"
    build:
      type: dockerfile
    ports:
      - port: 8000
        http_path: /health
        protocol: http
    regions:
      - fra
    scalings:
      - min: 1
        max: 1
